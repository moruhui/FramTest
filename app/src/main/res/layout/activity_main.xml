<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">


    <LinearLayout
        android:id="@+id/lilay_down"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="-0px"
        android:orientation="vertical">


        <android.support.v4.view.ViewPager
            android:id="@+id/viewpager_vp"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/main2_index"
                style="@style/main2_tab_text"
                android:drawableTop="@drawable/selector_tab_main2_index"
                android:text="首页" />

            <TextView
                android:id="@+id/main2_order"
                style="@style/main2_tab_text"
                android:drawableTop="@drawable/selector_tab_main2_order"
                android:text="订单" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/main2_water"
                style="@style/main2_tab_text"
                android:drawableTop="@drawable/selector_tab_main2_water"
                android:text="水站" />

            <TextView
                android:id="@+id/main2_person"
                style="@style/main2_tab_text"
                android:drawableTop="@drawable/selector_tab_main2_person"
                android:text="我的" />

        </LinearLayout>

    </LinearLayout>

    <TextView
        android:id="@+id/main2_down_order"
        style="@style/main2_tab_downorder"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@color/transparent"
        android:drawableTop="@drawable/ic_down_order"
        android:gravity="center"
        android:text="一键下单" />

</RelativeLayout>
